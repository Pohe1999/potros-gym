services:
  - type: web
    name: potros-gym-backend
    runtime: node
    plan: free
    region: oregon
    branch: main
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    env: node
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
