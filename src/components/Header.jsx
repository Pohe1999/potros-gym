// Header.jsx — v3.0.0 (solo referencia; el layout principal usa Sidebar + App.jsx topbar)
// Se conserva para compatibilidad pero no se renderiza directamente en App.jsx v3
export default function Header() { return null }
